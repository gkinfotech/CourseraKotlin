fun main(){
    //val list = listOf<Char>('a','b','c').joinToString(separator = "",prefix = "(",postfix = ")")
    println(listOf('a','b','c').joinToString(separator = "",prefix = "(",postfix = ")"))

}